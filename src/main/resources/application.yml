spring:
  application:
    name: spring-batch-study
  datasource:
    url: jdbc:mysql://localhost:3306/wlwpfhbatch?serverTimezone=UTC&characterEncoding=UTF-8
    username: test_user
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  batch:
    jdbc:
      initialize-schema: always



